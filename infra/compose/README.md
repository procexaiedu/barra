# Compose

Stacks Docker usadas pelo deploy via Portainer.

- `stack.barra.yml`: stack principal.
- `env/`: exemplos de variaveis por servico, sem segredos.
- `traefik/`: configuracao estatica e dinamica do Traefik.
