"""Simulador de cliente dual-control (EVAL-12). NAO-GATE -- descoberta, nunca cutover.

Pacote: o loop.py usa imports relativos (`from . import atos`); por isso este __init__ existe.
atos.py e cliente.py tambem sao carregaveis por caminho (importlib) para os testes puros offline.
"""
